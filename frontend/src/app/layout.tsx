import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'UK GovWatch Pro - Find UK Government Opportunities',
  description: 'AI-powered platform that helps UK businesses discover government contracts, grants, and opportunities from 350+ sources including all local councils.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}